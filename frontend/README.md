Frontend

React + TypeScript frontend. Integrates MediaPipe for pose extraction and streams pose frames via WebSocket to the backend.

Planned structure:
- `src/` - React source
- `public/` - static assets
- `tsconfig.json`, `package.json`, `tailwind.config.js`
