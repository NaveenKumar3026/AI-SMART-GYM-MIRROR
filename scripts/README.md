Scripts

Utility scripts for setup, database migrations, and developer tooling.

Examples:
- `scripts/bootstrap.sh` - create virtualenv, install deps
- `scripts/init-db.sh` - create DB and run migrations
